<h1>Admin Users</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="Nombre">#</th>
      <th scope="Email">First</th>
      <th scope="Admin">Administrador</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user|%>
    <tr>
      <th scope="row"><%= user.name%></th>
      <td><%= user.email%></td>
      <td><%= user.is_admin? ? "sí" : "no"%></td>
      <td><%= link_to "Editar", edit_user_path(user), class: "btn btn-warning" %></td>
    </tr>
    <%end%>
  </tbody>
</table>
